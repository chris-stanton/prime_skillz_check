// 1 - Write a for loop that runs 5 times
// Each iteration will increase the value
// of checkz by 1
var checkz = 0;

for (var i = 0; i < checkz.length; i++) {
  var sum += checkz;
  return sum;
}

// 2 - Write a while loop that runs
// 3 times. Each iteration will decrease
// the value of checkz by -2
var i = 0;
while (i == -6) {
  i -= 2;
  i--;
}

// 3 - Summarize in English what this code is doing.
// Use a multiline comment to do so.
//

// var numbers is set to an array of numbers
var numbers = [1, 2, 3, 4, 5, 6, 7];

//var total is equal to 0
var total = 0;

//for loop will run 6 times *incrumenting total by adding the index value everytime
for (var i = 0; i < numbers.length; i++) {
  total += numbers[i];
}
//console logging variable total
console.log(total);
